{
  "name": "valorant-account-manager",
  "version": "0.1.0",
  "private": true,
  "description": "A local, read-only Valorant store viewer for the active Riot Client session.",
  "main": "electron/main.js",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"vite\" \"wait-on tcp:5173 && cross-env VITE_DEV_SERVER_URL=http://localhost:5173 electron .\"",
    "build": "vite build",
    "start": "electron .",
    "check": "node --check electron/main.js && node --check electron/preload.cjs && node --check electron/ipcHandlers.js && node --check electron/riot/lockfile.js && node --check electron/riot/auth.js && node --check electron/riot/store.js && node --check electron/riot/contentCache.js && node --check electron/accounts/accountStore.js && node --check electron/accounts/accountService.js && node --check electron/accounts/riotClient.js && node --check electron/accounts/tcnoImport.js && node --check electron/accounts/switcher.js"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "latest",
    "concurrently": "latest",
    "cross-env": "latest",
    "electron": "latest",
    "vite": "latest",
    "wait-on": "latest"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest"
  }
}
